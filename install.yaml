
name: ibexa_cloud
pre_install_actions:

project_files:
  - providers/ibexa_cloud.yaml
  - web-build/Dockerfile.ibexa_cloud
  - config.ibexa_cloud.yaml

global_files:
dependencies:

post_install_actions:
removal_actions:

yaml_read_files:

